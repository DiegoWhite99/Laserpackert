<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Sin permisos de Bluetooth/ubicación a propósito: esta app no habla con
        la máquina. Genera una imagen y se la pasa a la app oficial de
        LaserPecker, que es la que ya tiene el emparejamiento Bluetooth hecho.
    -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_divergency"
        android:roundIcon="@drawable/logo_divergency"
        android:label="@string/app_name"
        android:theme="@style/Theme.EnviarLaserPecker"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
